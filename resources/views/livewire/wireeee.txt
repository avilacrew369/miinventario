   <!-- @forelse ($products as  $index => $product)
                        <tr class="border-b">
                            <td class="py-2 px-4">{{ $product['name'] }}</td>
                            <td class="py-2 px-4">
                                <x-wire-input
                                    wire:model.live="products.{{ $index }}.quantity"
                                    type="number"
                                    class="w-10"
                                />
                            </td>
                            <td class="py-2 px-4">
                                 <x-wire-input
                                    wire:model.live="products.{{ $index }}.price"
                                    type="number"
                                    class="w-10"
                                    step="0.01"
                                />
                            </td>
                            <td class="py-2 px-4">{{ (float)($product['quantity'] ?? 0) * (float)($product['price'] ?? 0) }}</td>
                            <td class="py-2 px-4">
                                <x-wire-mini-button 
                                    wire:click="removeProduct({{ $loop->index }})"
                                    color="red"
                                    size="rounded"
                                    icon="trash"
                                    >
                                    Eliminar
                                </x-wire-mini-button>
                            </td>
                        </tr>
                        
                    @empty -->
                        
                    <!-- @endforelse -->